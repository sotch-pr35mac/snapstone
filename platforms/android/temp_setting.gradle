android {
            defaultConfig {
                applicationId = "org.kzoo.snapstone"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }